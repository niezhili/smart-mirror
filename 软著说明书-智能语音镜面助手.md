# 智能语音镜面助手 V1.0 — 软件说明书

---

## 一、概述

智能语音镜面助手（Smart Voice Mirror Assistant）是一款运行在树莓派上的 AI 语音交互智能镜面系统。软件将传统镜面与信息显示、语音对话、智能家居控制、人脸识别等功能深度融合，为用户提供自然便捷的人机交互体验。

**软件名称**：智能语音镜面助手
**版本号**：V1.0
**运行平台**：Raspberry Pi OS / Windows / macOS
**主要编程语言**：Python、JavaScript
**目标硬件**：树莓派 + 单向透视镜 + 显示屏 + 麦克风 + 摄像头 + 传感器（PIR红外、DHT11温湿度、红外发射管）

本软件适用于家庭、办公等场景，用户可通过语音对话查询天气、新闻、日历等信息，控制空调等智能家居设备，同时镜面实时显示时间、室内温湿度、新闻头条等实用信息。

---

## 二、功能模块

本软件由前端信息展示系统和后端 AI 语音交互系统两大部分组成，包含以下核心功能模块：

### 2.1 语音交互模块

语音交互是本软件的核心功能，实现了完整的"唤醒 → 识别 → 理解 → 回复"闭环：

- **语音唤醒**：支持多个自定义唤醒词（如"你好"、"小镜子"等），系统在后台持续监听，识别到唤醒词后自动进入对话模式。
- **语音识别（ASR）**：集成阿里云 DashScope Paraformer 实时语音识别模型和百度语音识别服务，支持中文、英文、日文三种语言的语音转文字，适应多语种使用场景。
- **大语言模型对话（LLM）**：接入阿里云通义千问（Qwen）大语言模型，系统设定为一位知识渊博、精通中英日三语的智能助手角色，支持自然语言理解与多轮上下文对话。
- **语音合成（TTS）**：采用字节跳动火山引擎 WebSocket 实时语音合成服务，支持中文、英文、日文、德文、法文五种语言的语音输出，提供多种音色和情感风格（如愉悦、悲伤、故事讲述等），并实现了流式分段播放技术，大幅降低语音响应延迟。

### 2.2 人脸识别模块

基于 dlib 深度学习模型的人脸识别功能：

- **人脸注册**：支持从 `known_faces/` 目录加载已注册用户的人脸照片，自动提取人脸特征编码。
- **实时识别**：通过摄像头实时采集画面，检测人脸后进行特征比对，识别成功后通过语音播报个性化欢迎语（如"你好，小明！"）。
- **智能触发**：结合 PIR 人体感应模块，当用户靠近镜面时自动启动人脸识别流程，避免持续进行无效识别，节约系统资源。

### 2.3 信息展示模块

前端界面基于 MagicMirror² 框架进行深度定制，提供丰富的信息展示功能：

- **时钟显示**：支持数字时钟和模拟时钟两种显示模式，可配置 12/24 小时制，支持多时区显示和日期（公历+星期）展示。
- **天气信息**：接入和风天气（QWeather）API，实时显示当前天气状况、温度、湿度、风向风力、体感温度等，温度采用颜色分级显示（低温蓝色、高温红色）。
- **新闻滚动**：接入聚合数据新闻 API，以滚动轮播方式展示国内外新闻头条，包含标题、分类、来源和发布时间，支持定时更新（每日多时段自动刷新）。
- **室内温湿度**：通过 DHT11/DHT22 传感器读取室内实时温湿度数据，温度与湿度均采用颜色分级显示，直观反映室内环境舒适度。
- **日历与节日**：支持 iCalendar 格式日历订阅，显示日程安排；背景图片模块内置中国传统二十四节气和传统节日识别功能，在对应日期自动切换主题背景图。

### 2.4 智能家居控制模块

- **空调语音控制**：用户通过语音指令（如"打开空调""温度调到26度""切换制冷模式"等）控制空调。系统通过涂鸦云 API 获取红外码库，经树莓派 GPIO 红外发射管发送控制信号。
- **支持的操作**：电源开/关、温度调节（16-30℃）、模式切换（制冷/制热/自动/除湿/送风）、风速调节（自动/低/中/高）。

### 2.5 人体感应模块

- **PIR 红外检测**：通过连接到树莓派 GPIO 17 引脚的 PIR 人体红外传感器检测用户靠近。
- **智能联动**：检测到用户后自动播报引导语音（提示用户面对摄像头），并触发人脸识别流程。用户离开超过 10 分钟后系统自动返回待机/唤醒词监听状态，实现无感节能。

### 2.6 多语言支持模块

- **界面语言切换**：前端支持中文/英文一键切换，切换后页面自动刷新应用对应语言包，语言偏好保存至本地存储和服务端配置文件。
- **语音多语种支持**：ASR 支持中/英/日三语识别，TTS 支持中/英/日/德/法五语合成，LLM 系统提示词设定为三语助手，确保多语言场景下的连贯体验。

---

## 三、技术实现

### 3.1 系统架构

本软件采用前后端分离架构：

```
┌─────────────────────────────────────────────────┐
│                   前端展示层                       │
│  (Electron + Express + Socket.io + 浏览器渲染)     │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌───────┐ │
│  │ 时钟  │ │ 天气  │ │ 新闻  │ │ 日历  │ │ 背景图 │ │
│  └──────┘ └──────┘ └──────┘ └──────┘ └───────┘ │
│  ┌──────┐ ┌──────┐ ┌──────────────────────────┐ │
│  │温湿度 │ │语言切换│ │  各个模块通过 Socket.io   │ │
│  └──────┘ └──────┘ │  与后端实时双向通信        │ │
│                     └──────────────────────────┘ │
└──────────────────────┬──────────────────────────┘
                       │
┌──────────────────────┴──────────────────────────┐
│                   后端 AI 层                      │
│  (Python + Flask + 多线程/异步)                    │
│  ┌────────┐ ┌──────┐ ┌──────┐ ┌─────────────┐  │
│  │VAD 检测│→│ASR识别│→│LLM对话│→│TTS 合成/播放 │  │
│  └────────┘ └──────┘ └──────┘ └─────────────┘  │
│  ┌────────────┐ ┌──────────┐ ┌──────────────┐  │
│  │ 人脸识别    │ │人体感应   │ │ 智能家居控制   │  │
│  └────────────┘ └──────────┘ └──────────────┘  │
│  ┌──────────────────────────────────────────┐   │
│  │         天气服务 / 配置管理 / 日志系统      │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
```

### 3.2 技术栈

| 层级 | 技术 |
|------|------|
| 前端框架 | Electron + Express + Socket.io |
| 前端渲染 | HTML5 + CSS3 + Nunjucks 模板引擎 |
| 后端框架 | Python + Flask + PyQt5/Kivy |
| 语音活动检测 | Silero VAD (PyTorch) |
| 语音识别 | 阿里云 DashScope Paraformer / 百度语音识别 |
| 语音合成 | 字节跳动火山引擎 WebSocket TTS / 百度语音合成 |
| 大语言模型 | 阿里云通义千问 (Qwen-Turbo) |
| 人脸识别 | face_recognition (dlib CNN/HOG) + OpenCV |
| 硬件控制 | RPi.GPIO (PIR传感器、DHT11、红外发射) |
| 智能家居 | 涂鸦云 API (红外码库) |
| 天气数据 | 和风天气 QWeather API |
| 新闻数据 | 聚合数据 API |
| 配置管理 | YAML + PyYAML |
| 日志系统 | Loguru |

### 3.3 核心算法与实现

**语音活动检测（VAD）算法**：
采用 Silero VAD 预训练模型，以 16kHz 采样率实时分析麦克风输入的音频帧（每帧 512 采样点），计算语音概率。当连续检测到高于置信度阈值（默认 0.3）的语音帧达到最小连续帧数要求后开始录音，检测到静音超过指定时长（默认 1 秒）后结束录音，将音频保存为 WAV 文件供后续识别。

**流式 TTS 播放技术**：
针对长文本语音合成延迟高的问题，本系统实现了智能文本分段 + 流式播放架构。首先按语言类型和标点符号将回复文本切分为最多 48 字符的短句，然后通过线程池并发请求 TTS 服务生成各段音频，同时由 `StreamingOrderedPlayer` 按原文顺序流式播放——即第一段音频生成完毕后立即开始播放，后续段落边生成边排队，显著降低用户感知的响应延迟（首音节响应时间从数秒压缩至毫秒级）。

**面部识别流程**：
人脸注册阶段，对已知人员照片进行预处理（缩放至最大 1024px、定位人脸区域），使用 dlib 的 CNN 模型（GPU 可用时）或 HOG 模型（CPU 模式）提取 128 维面部特征编码并持久化存储。识别阶段，摄像头实时捕获画面，每 3 帧处理一次（0.5x 缩放加速），将检测到的人脸编码与已知特征库进行欧氏距离比对，选取最佳匹配结果。

**红外空调控制协议**：
参考通用红外遥控协议实现 NEC 编码格式的 38kHz 载波调制。发送端通过树莓派 GPIO 18 引脚的 PWM 硬件模块输出精确时序信号：9ms 载波 + 4.5ms 间隙作为引导码，数据位以 600μs 载波脉冲表示逻辑 1（后跟 1800μs 间隙）或逻辑 0（后跟 600μs 间隙），末尾以 600μs 结束码收尾。指令码通过涂鸦云红外码库 API 在线获取。

### 3.4 外部 API 与云服务

| 服务类型 | 服务商 | 接口/模型 |
|----------|--------|-----------|
| 语音识别 | 阿里云 DashScope | paraformer-realtime-v2 |
| 语音识别（备选） | 百度 AI | 百度语音识别 REST API |
| 语音合成 | 字节跳动火山引擎 | WebSocket TTS v1/ws_binary |
| 大语言模型 | 阿里云 DashScope | qwen-turbo |
| 天气 | 和风天气 | /v7/weather/now |
| 新闻 | 聚合数据 | 新闻头条 API |
| 智能家居 | 涂鸦云 | 红外码库 API |
| 地理位置 | ipapi.co / geocoder | IP 定位 |

---

## 四、操作流程

### 4.1 系统启动

1. 树莓派上电后，系统自动启动并进入待机状态。
2. 前端界面全屏显示，默认展示：模拟/数字时钟（中央）、天气信息（右上）、新闻滚动条（右侧）、室内温湿度（左下）、背景图（全屏底层）。
3. 后端在后台启动 Flask 服务器（端口 8080）、加载人脸特征库、初始化各硬件模块，并开始循环监听唤醒词。

### 4.2 用户靠近与身份识别

1. 用户走近镜面 → PIR 红外传感器检测到人体活动。
2. 系统通过 TTS 播报引导语音（如"请面对摄像头"）。
3. 摄像头启动人脸识别流程，获取当前用户面部图像。
4. 将检测到的人脸特征与已注册人脸库进行比对匹配。
5. 匹配成功后，系统播报个性化欢迎语（如"你好，小明！"）；匹配失败则进入访客模式。

### 4.3 语音交互流程

1. **唤醒**：用户说出唤醒词（如"你好"）→ 系统检测到唤醒词后进入对话模式（可配合提示音）。
2. **语音输入**：用户说出指令或问题 → VAD 检测语音活动并自动录音 → ASR 将音频转为文字。
3. **意图处理**：
   - 若识别为**关键词指令**（如"天气""时间""空调""再见"），系统直接匹配对应处理逻辑：
     - "天气" → 通过 IP 定位获取当前城市天气并语音播报
     - "时间" → 播报当前日期时间
     - "空调+指令" → 调用空调控制模块
     - "再见""关机" → 退出对话模式或关闭系统
   - 若不属于关键词指令，文本送入 LLM 进行自然语言理解和生成回复。
4. **语音回复**：系统将 LLM 生成或预定义的回复文本通过流式 TTS 合成为语音并播放。
5. **多轮对话**：对话上下文由 ContextManager 维护，支持连续多轮交互。
6. **退出对话**：用户说"再见"或离开超过 10 分钟无交互，系统返回待机/唤醒词监听状态。

### 4.4 智能家居控制流程

1. 用户唤醒系统后发出空调控制语音指令，如"把空调调到 26 度"。
2. ASR 将语音转为文字，系统解析出操作类型（温度调节）、目标参数（26℃）。
3. 通过涂鸦云 API 查询对应品牌空调的红外码。
4. 经树莓派 GPIO 18 引脚的红外发射管发送编码信号。
5. 系统通过 TTS 播报操作结果（如"已将温度设置为 26 度"）。

### 4.5 语言切换操作

1. 用户点击屏幕右下角的语言切换按钮。
2. 系统将语言偏好写入浏览器 localStorage 和服务器 `language-state.json` 文件。
3. 页面自动刷新，所有模块加载对应语言的翻译文本，完成界面语言切换。

---

## 五、创新点

### 5.1 多模态自然交互

本系统创新性地融合了**人体红外感应 + 人脸识别 + 语音唤醒**三种感知模态，实现了层次化的智能唤醒机制。用户无需任何物理接触——走近即感知、识别即问候、说话即交互，打造了真正"无感"的智能体验。三层感知依次递进，避免了单一模态的误触发和漏触发问题。

### 5.2 流式多语言 TTS 播放架构

与传统的"全文合成再播放"模式不同，本系统实现了**智能文本分段 + 并发合成 + 有序流式播放**的三段式架构。文本按语言边界和语义标点进行智能切分，通过线程池并发请求各段音频，播放器按原文顺序无缝衔接播放。该架构将用户感知的首音节响应延迟降低至毫秒级，同时支持中、英、日、德、法五种语言及多种情感风格的语音输出。

### 5.3 中国文化特色适配

前端背景图模块内置了**中国传统二十四节气和传统节日**（春节、端午、中秋、重阳等）的日期识别功能，在节气或节日当天自动切换对应的主题背景图，体现了传统文化与智能硬件的融合。同时系统全面支持中英文双语的界面与语音交互，适合中国家庭使用场景。

### 5.4 边缘计算架构

系统核心控制逻辑、人脸识别、VAD 语音检测、红外遥控信号生成等计算密集型任务均在树莓派本地完成，仅在需要云端 AI 能力（LLM 对话、ASR 识别、TTS 合成）时才调用云服务 API。这种边缘+云协同的架构设计兼顾了响应速度和智能水平，在网络不稳定时仍可维持基本的本地功能运行。

---

> **文档版本**：V1.0
> **编制日期**：2026年5月31日
